export default function initStorage() {
    /*
    let input_value = $(".section-writeUs__form_email").val()
    sessionStorage.setItem('storage', input_value);
    $(".section-writeUs__form_email").val(sessionStorage.getItem('storage'));
     */
}